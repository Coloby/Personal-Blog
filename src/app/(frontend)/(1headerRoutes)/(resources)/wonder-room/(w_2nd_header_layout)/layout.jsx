import FilterSearchHeader from "@/features/cards/components/layout/FilterSearchHeader"

const layout = ({ children }) => {

  return (
    <>
      <FilterSearchHeader children={children} />
    </>
  )
}

export default layout